[project]
name = "simulation"
version = "0.1.0"
description = "Simulation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "casadi>=3.8.1",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
]
